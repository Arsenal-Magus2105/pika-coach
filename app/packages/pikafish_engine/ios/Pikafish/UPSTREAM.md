# Pikafish source

This directory is populated by `scripts/fetch_pikafish_source.sh` from the
official `official-pikafish/Pikafish` repository at the commit pinned in that
script. Generated source is intentionally excluded from Git.
